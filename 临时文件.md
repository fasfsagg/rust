# Development Preferences
- User works on Windows 10 x86 system with Rust 2024 Edition and Axum 0.8.4 framework at D:\ceshi\ceshi\axum-tutorial
- User requires mandatory Context7 MCP verification before writing any code or fixing errors
- User follows strict 5-step workflow: TaskMaster AI analysis → Context7 MCP verification → TDD implementation → quality validation → task completion recording
- User requires enterprise-grade code quality: TDD approach, >95% test coverage with cargo llvm-cov HTML reports, SOLID/DRY principles, small iterative changes
- User prefers using package managers (cargo update/outdated) instead of manual config editing, avoids accessing crates.io pages to prevent AI getting stuck
- User requires comprehensive quality verification: cargo check/clippy/test validation, functional API testing, honest reporting of issues rather than claiming completion without verification
- User prefers PowerShell syntax for environment variables on Windows: use $env:RUST_LOG="debug" instead of RUST_LOG=debug for cargo test commands.
- User prefers to try simpler/incremental solutions (approach A) before attempting more complex/comprehensive solutions (approach B).
- User prefers systematic approach with risk control measures: Git commits before major changes, step-by-step validation after each phase, and comprehensive testing of core functionality before proceeding to next stage.
- User prefers using task-master-ai for task initialization and breakdown.
- User prefers chat-focused three-column layout with ratios like 0.8fr:1fr:1.5fr (auth:tasks:chat) to emphasize chat functionality as the main area, and requires complete chat input functionality with proper WebSocket integration testing.

# Development Workflow
- User uses MCP tool verification before writing code.
- User has a specific workflow for handling Rust/Clippy warnings: identify warning type, use Context7 MCP for Axum 0.8.4 documentation queries, create solutions following TDD/minimal changes/strict validation principles, make code modifications with str-replace-editor, and verify with tests.
- User prefers systematic error analysis approach: compile verification → error classification (syntax/module/dependency/API) → prioritized fixing (imports first, then Context7 MCP verification) → quality validation → status updates.
- User completed Task 1 TDD testing with 87.04% line coverage, 81.48% function coverage, 8.8/10 enterprise readiness score, and E2E test pass rate improved to 75.4% (43/57 tests), with main remaining issue being task list UI display problems affecting #taskList element rendering.
- User wants to plan PostgreSQL+DragonflyDB migration after testing completion, has Podman+PostgreSQL+DragonflyDB environment ready with docker-compose.yml.

# Project Specifics
- Building enterprise-level million-concurrent chat application backend using Axum framework
- User is migrating Axum chat project from SQLite to PostgreSQL+DragonflyDB architecture with Podman environment.
- Database uses 'user' table (not 'users')
- WebSocket functionality with JWT authentication and comprehensive security testing
- Performance targets: >100k QPS throughput, million-concurrent scalability
- User uses SeaORM with native-tls backend to avoid compilation problems.

# Testing
- TDD approach with mandatory unit/integration tests for all functions/modules
- >95% code coverage target using cargo llvm-cov HTML reports
- E2E testing with Playwright MCP for complete user scenarios
- Comprehensive regression testing with cargo check/clippy/fmt validation
- Server runs on port 3000, keep server running during tests with separate terminal windows
