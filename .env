# .env
# .env
#
# Axum 应用环境变量配置文件
#
# 【注意】:
# 1. 这个文件应该被添加到 .gitignore 中，以避免将敏感信息（如密码）提交到版本控制。
# 2. 修改此文件后，需要重启应用程序才能使更改生效。

# --- HTTP 服务器配置 ---
# 服务器监听的地址和端口。
# 必须使用 IP 地址，而不是 "localhost"。
HTTP_ADDR="127.0.0.1:3000"

# --- 数据库配置 ---
# 数据库连接字符串。
# 对于 SQLite, `?mode=rwc` 会在文件不存在时自动创建它。
DATABASE_URL="sqlite://task_manager.db?mode=rwc"

# 数据库连接池最大连接数
MAX_CONNECTIONS=20

# 建立新连接的超时时间 (秒)
CONNECT_TIMEOUT_SECS=5

# 从连接池获取一个连接的超时时间 (秒)
ACQUIRE_TIMEOUT_SECS=5

# --- 日志配置 ---
# RUST_LOG 控制日志输出的级别。
# 例如: "info", "debug", "warn", "error"
# "info,tower_http=debug" 表示默认级别是 info，但 tower_http crate 的日志级别是 debug。
RUST_LOG="info,tower_http=debug,sea_orm=debug"