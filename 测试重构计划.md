# 测试文件重构计划

## 🎯 重构目标

1. **清理技术债务**: 删除所有过时的测试文件
2. **适配新架构**: 为 PostgreSQL + DragonflyDB 设计新的测试策略
3. **提升测试质量**: 采用更现代的测试模式和工具
4. **保持高覆盖率**: 目标 >95% 代码覆盖率

## 📋 执行步骤

### 阶段1: 清理现有测试文件 (立即执行)

**删除所有测试文件**:
```bash
# 删除 tests/ 目录下的所有文件
rm -rf tests/*
```

**保留的测试基础设施**:
- `src/test_config.rs` (已修复)
- `src/test_utils.rs` (已修复)
- 测试相关的 Cargo.toml 配置

### 阶段2: 重新设计测试架构 (下一步)

**新的测试文件结构**:
```
tests/
├── unit/                    # 单元测试
│   ├── config_tests.rs     # 配置模块测试
│   ├── auth_tests.rs       # 认证模块测试
│   └── utils_tests.rs      # 工具模块测试
├── integration/             # 集成测试
│   ├── api_tests.rs        # API 端点测试
│   ├── database_tests.rs   # 数据库集成测试
│   └── websocket_tests.rs  # WebSocket 集成测试
├── e2e/                    # 端到端测试
│   ├── user_journey_tests.rs
│   └── performance_tests.rs
└── fixtures/               # 测试数据和工具
    ├── test_data.rs
    └── mock_services.rs
```

### 阶段3: 数据库测试策略 (PostgreSQL + DragonflyDB)

**测试数据库配置**:
- 使用 Docker Compose 启动测试数据库
- PostgreSQL: 主数据存储测试
- DragonflyDB: 缓存和会话测试
- 每个测试使用独立的数据库/命名空间

**测试数据管理**:
- 使用事务回滚确保测试隔离
- 提供测试数据工厂函数
- 支持并行测试执行

### 阶段4: 现代化测试工具链

**推荐的测试工具**:
- `cargo-nextest`: 更快的测试运行器
- `cargo-llvm-cov`: 代码覆盖率 (已配置)
- `testcontainers`: Docker 容器测试
- `wiremock`: HTTP 服务模拟 (已配置)
- `criterion`: 性能基准测试 (已配置)

## 🔧 立即行动建议

### 1. 删除现有测试文件
```bash
# 在项目根目录执行
Remove-Item -Recurse -Force tests\*
```

### 2. 创建新的测试基础
```bash
# 创建新的测试目录结构
mkdir tests\unit
mkdir tests\integration  
mkdir tests\e2e
mkdir tests\fixtures
```

### 3. 更新 Cargo.toml 测试配置
- 添加 testcontainers 依赖
- 配置测试数据库环境
- 设置并行测试参数

## ✅ 优势分析

**删除重构的优势**:
1. **清理技术债务**: 避免维护过时的测试代码
2. **适配新架构**: 为 PostgreSQL + DragonflyDB 设计专门的测试
3. **提升开发效率**: 新测试更容易维护和扩展
4. **现代化工具链**: 使用最新的测试工具和模式
5. **更好的测试隔离**: 避免测试间的相互影响

**风险控制**:
1. **保留核心测试逻辑**: 从现有测试中提取有价值的断言和场景
2. **分阶段重建**: 优先重建核心功能测试
3. **持续集成**: 确保每次提交都有基本的测试覆盖

## 🚀 下一步行动

1. **确认删除**: 您确认删除所有测试文件后，我立即执行
2. **设计新架构**: 基于 PostgreSQL + DragonflyDB 设计测试策略
3. **重建核心测试**: 优先重建配置、认证、API 等核心模块测试
4. **集成 CI/CD**: 确保新测试在持续集成中正常运行

**您是否同意立即删除所有测试文件并开始重构？**
