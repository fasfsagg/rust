# PostgreSQL + DragonflyDB 迁移执行计划

## 🎯 迁移目标

1. **PostgreSQL**: 主数据存储 (用户、任务、配置等)
2. **DragonflyDB**: 高性能缓存和会话存储
3. **保持现有功能**: 确保所有 API 功能正常
4. **性能提升**: 利用 Redis 兼容的 DragonflyDB 提升缓存性能

## 📋 详细执行步骤

### 第1步: 环境准备 (1-2小时)

**1.1 Docker Compose 配置**
```yaml
# docker-compose.yml (更新现有文件)
version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: axum_tutorial
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql

  dragonflydb:
    image: docker.dragonflydb.io/dragonflydb/dragonfly:latest
    ports:
      - "6379:6379"
    command: ["--logtostderr", "--alsologtostderr"]
    volumes:
      - dragonfly_data:/data

volumes:
  postgres_data:
  dragonfly_data:
```

**1.2 依赖项更新**
```toml
# Cargo.toml 更新
[dependencies]
# PostgreSQL 支持
sea-orm = { version = "1.1.12", features = ["sqlx-postgres", "runtime-tokio-native-tls", "mock"] }
sqlx = { version = "0.8", features = ["postgres", "runtime-tokio-native-tls", "chrono", "uuid"] }

# Redis/DragonflyDB 支持
redis = { version = "0.26", features = ["tokio-comp", "connection-manager"] }
# 或者使用更现代的客户端
fred = { version = "9.0", features = ["enable-rustls"] }

# 连接池管理
deadpool-postgres = "0.14"
deadpool-redis = "0.16"
```

### 第2步: 数据库配置重构 (2-3小时)

**2.1 更新配置结构**
```rust
// src/config.rs 更新
#[derive(Debug, Clone)]
pub struct DatabaseConfig {
    pub postgres_url: String,
    pub redis_url: String,
    pub max_postgres_connections: u32,
    pub max_redis_connections: u32,
    pub connection_timeout: Duration,
}

#[derive(Debug, Clone)]
pub struct AppConfig {
    // ... 现有字段
    pub database: DatabaseConfig,
    // 移除旧的 database_url 字段
}
```

**2.2 环境变量配置**
```bash
# .env 文件更新
DATABASE_URL=postgresql://postgres:password@localhost:5432/axum_tutorial
REDIS_URL=redis://localhost:6379
MAX_POSTGRES_CONNECTIONS=20
MAX_REDIS_CONNECTIONS=10
```

### 第3步: 数据库连接管理重构 (3-4小时)

**3.1 创建双数据库连接管理器**
```rust
// src/database/mod.rs (新文件)
pub mod postgres;
pub mod redis;
pub mod manager;

use sea_orm::DatabaseConnection;
use redis::aio::ConnectionManager;

#[derive(Clone)]
pub struct DatabaseManager {
    pub postgres: DatabaseConnection,
    pub redis: ConnectionManager,
}
```

**3.2 更新启动逻辑**
```rust
// src/startup.rs 更新
pub async fn init_app(config: AppConfig) -> Result<(Router, DatabaseManager)> {
    // 初始化 PostgreSQL 连接
    let postgres = Database::connect(&config.database.postgres_url).await?;
    
    // 初始化 Redis/DragonflyDB 连接
    let redis_client = redis::Client::open(config.database.redis_url)?;
    let redis = ConnectionManager::new(redis_client).await?;
    
    let db_manager = DatabaseManager { postgres, redis };
    
    // 运行数据库迁移
    Migrator::up(&postgres, None).await?;
    
    let app = create_router(db_manager.clone());
    Ok((app, db_manager))
}
```

### 第4步: 数据访问层重构 (4-5小时)

**4.1 更新实体模型**
- 保持现有的 SeaORM 实体定义
- 添加 Redis 缓存键定义
- 更新迁移文件以支持 PostgreSQL

**4.2 实现缓存策略**
```rust
// src/app/service/cache_service.rs (新文件)
pub struct CacheService {
    redis: ConnectionManager,
}

impl CacheService {
    pub async fn get_user_session(&self, token: &str) -> Result<Option<UserSession>> {
        // 从 DragonflyDB 获取会话
    }
    
    pub async fn set_user_session(&self, token: &str, session: &UserSession) -> Result<()> {
        // 存储会话到 DragonflyDB
    }
    
    pub async fn invalidate_user_session(&self, token: &str) -> Result<()> {
        // 删除会话
    }
}
```

### 第5步: API 层适配 (2-3小时)

**5.1 更新状态管理**
```rust
// src/app/state.rs 更新
#[derive(Clone)]
pub struct AppState {
    pub db_manager: DatabaseManager,
    // ... 其他状态
}
```

**5.2 更新控制器**
- 将所有 `DatabaseConnection` 替换为 `DatabaseManager`
- 添加缓存逻辑到关键操作
- 更新认证中间件使用 Redis 会话

### 第6步: 测试和验证 (2-3小时)

**6.1 手动测试**
- 启动 Docker Compose 环境
- 验证所有 API 端点功能
- 检查数据持久化和缓存功能

**6.2 性能验证**
- 对比迁移前后的响应时间
- 验证缓存命中率
- 检查数据库连接池状态

## ⚠️ 风险控制

1. **备份现有数据**: 虽然使用内存数据库，但要备份重要的配置和测试数据
2. **渐进式迁移**: 先迁移非关键功能，再迁移核心功能
3. **回滚计划**: 保留 SQLite 配置作为备用方案
4. **监控指标**: 添加数据库连接和缓存的监控指标

## 🎯 成功标准

1. ✅ 所有现有 API 功能正常工作
2. ✅ 数据正确存储到 PostgreSQL
3. ✅ 会话和缓存正确存储到 DragonflyDB
4. ✅ 响应时间有明显改善
5. ✅ 数据库连接池稳定运行

## 📅 预计时间

- **总时间**: 12-18 小时
- **关键路径**: 数据库连接管理 → API 层适配 → 测试验证
- **可并行**: 环境准备和配置重构可以并行进行

**您是否同意按此计划先进行数据库迁移？**
